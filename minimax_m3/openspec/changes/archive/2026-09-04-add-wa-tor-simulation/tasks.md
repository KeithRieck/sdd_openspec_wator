## 1. Configuration and project skeleton

- [x] 1.1 Create `src/config.js` exporting grid dimensions (100×70), densities (0.30, 0.05), breed times (3, 25), shark energy values (initial 5, gain 3, cost 1), colors (fish green, shark blue, water background), and speed options (`[1, 5, 10, 30, 60]` with default 10). Verify by importing the module in a temporary `node --input-type=module -e "import('./src/config.js').then(m => console.log(m.DEFAULT_SPEED))"` call from the repo root and observing `10`.
- [x] 1.2 Create `index.html` with the Phaser 4 CDN script tag (`https://cdn.jsdelivr.net/npm/phaser@4.1.0/dist/phaser.min.js`) and an ES2020 module entry point that imports `src/main.js`. Verify by opening `index.html` in a browser and confirming Phaser is defined on `window` (DevTools console: `typeof Phaser === 'object'` returns `true`).

## 2. Simulation engine

- [x] 2.1 Implement `src/simulation/Grid.js` with `width`, `height`, `cellAt(x, y)`, `place(entity, x, y)`, `remove(entity)`, `orthogonalNeighbors(x, y)` (toroidal wrapping), and `findById(id)`. Verify by writing a temporary Node script that constructs a 3×3 grid, places two entities, and asserts `orthogonalNeighbors(1, 1)` returns 4 cells, `orthogonalNeighbors(0, 0)` returns `[(1,0),(2,0),(0,1),(0,2)]` (toroidal wrap), and `findById` returns the correct entity after a `remove`.
- [x] 2.2 Implement `src/simulation/Fish.js` with `id`, `x`, `y`, `breedAge`, and an `act(grid)` method that moves to a random adjacent empty cell and, if breeding-ready, leaves a newborn on the old cell and resets the parent's `breedAge` to 0. Verify by writing a temporary Node script that creates a Fish on a fresh grid, calls `act()`, and asserts the fish moved to an adjacent cell and `breedAge` advanced; then a second call with `breedAge` at threshold leaves a newborn in the old cell.
- [x] 2.3 Implement `src/simulation/Shark.js` with `id`, `x`, `y`, `breedAge`, `energy`, and an `act(grid)` method that decrements energy first, dies at zero, otherwise prefers an adjacent fish cell, otherwise a random adjacent empty cell, and breeds similarly to Fish with energy reset to `initialSharkEnergy` for newborns. Verify by writing a temporary Node script that constructs a Shark with energy 1, calls `act()`, and asserts the shark is removed; then constructs a Shark with energy 5 adjacent to a Fish, calls `act()`, and asserts the shark moved onto the fish cell and energy increased by `sharkEnergyGain`.
- [x] 2.4 Implement `src/simulation/WatorSimulation.js` with `chronon`, `running`, `speed`, the entity collections, `step()` (snapshot IDs, shuffle, iterate with dead-skip and newborn-deferral), `reset()`, `setRunning()`, `getFishCount()`, `getSharkCount()`, `getStatus()`, and `getHistorySamples()` (rolling 500-chronon ring buffer). Verify by writing a temporary Node script that constructs a 10×10 simulation with a few fish and sharks, calls `step()` 10 times, and asserts `chronon === 10`, the history length is 10, and total entity counts are non-negative integers.

## 3. Scenes and main entry

- [x] 3.1 Implement `src/scenes/BootScene.js` as a minimal scene whose `create()` immediately calls `this.scene.start('SimulationScene')`. Verify by launching the app and observing that the simulation scene appears (no black flash longer than one frame).
- [x] 3.2 Implement `src/main.js` to create a Phaser game with `BootScene` and `SimulationScene`, configured with no built-in background color (the scene draws its own water background). Verify by launching the app and observing the water-colored background fills the window with no white flash.
- [x] 3.3 Implement `src/scenes/SimulationScene.js` with the layout helper, stats text, world rendering (Phaser `Graphics` circles, no grid lines), the five speed buttons using `PhaserButton`, the Play/Pause / Step / Reset buttons, the chart renderer, and the accumulator-based timing in `update()`. Verify by launching the app and observing the world renders as green and blue circles on a water background, the stats panel shows live counts, the controls are clickable, and changing speed visibly changes the chronon rate.

## 4. Layout and responsive behavior

- [x] 4.1 Wire the layout helper to the Phaser `resize` event so that on window resize, the stats/world/controls/chart rectangles are recomputed while preserving the world aspect ratio. Verify by resizing the browser window from desktop (≥ 1024 wide) to iPad-mini dimensions (744×1133) and observing that all controls remain clickable and the world keeps its 10:7 ratio without distortion.

## 5. PWA shell

- [x] 5.1 Create `manifest.webmanifest` with `name`, `short_name`, `start_url: "/sdd_openspec_wator/minimax_m3/index.html"`, `scope: "/sdd_openspec_wator/minimax_m3/"`, `display: "standalone"`, and an `icons` array referencing `assets/icon-192.png` and `assets/icon-512.png`. Verify by serving the repo over a local HTTP server, opening DevTools → Application → Manifest, and confirming the manifest parses without errors and both icons are listed.
- [x] 5.2 Create `sw.js` that precaches the app shell (`index.html`, `manifest.webmanifest`, all `src/**/*.js` modules, and `assets/icon-*.png`) using a cache-first strategy, and registers the Phaser CDN URL as network-only. Verify by loading the app once online, then going offline (DevTools → Network → Offline) and reloading; the app shell loads and runs, with the simulation pausing on the first frame if Phaser fails to load.
- [x] 5.3 Register the service worker from `index.html` with scope `/sdd_openspec_wator/minimax_m3/`. Verify by loading the app and confirming in DevTools → Application → Service Workers that the SW is `activated and running`.

## 6. Manual verification

- [x] 6.1 Launch the app from a fresh browser profile and confirm it starts directly in a running simulation at `10x` speed with no landing page. Verify by observing the first paint shows a populated grid and the chronon counter is incrementing.
- [x] 6.2 Exercise Play/Pause, Step (while paused), Reset, and all five speed buttons (`1x`, `5x`, `10x`, `30x`, `60x`). Verify by observing correct toggling: Play/Pause label flips, Step is disabled while running and advances exactly one chronon when activated while paused, Reset clears the world and resumes at the selected speed, and speed changes take effect on subsequent updates without resuming a paused simulation.
- [x] 6.3 Trigger a terminal state by running until sharks (then fish) go extinct, and confirm the matching status text appears (`Sharks extinct`, `Fish extinct`, or `Ecosystem collapsed`), the simulation auto-pauses, and Play is disabled until Reset. Verify by observing the status panel text and that Play cannot resume the simulation without Reset.
- [x] 6.4 Resize the window from desktop width down to 744×1133 (iPad mini) and confirm the layout reflows while the world aspect ratio is preserved and all controls remain usable. Verify by clicking each control at the narrow viewport.
